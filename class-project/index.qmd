---
title: "Prenatal Mental Health Screening and Postpartum Depressive Symptoms"
author: "Alexandra Tejada-Strop"
bibliography: ../../assets/references/references.bib
csl: ../../assets/references/apa.csl

format:
  html:
    toc: true
    number-sections: true
    highlight-style: github

execute:
  echo: false
  warning: false
  message: false
  eval: true
---

```{r}
library(tidyverse)
library(here)
library(broom)
library(knitr)
library(scales)
library(survey)
library(kableExtra)

analytic_path <- here("data", "processed-data", "prams_analytic_localfix.rds")
stopifnot(file.exists(analytic_path))

analytic_dat <- readRDS(analytic_path)

n_total <- nrow(analytic_dat)

analytic_complete <- analytic_dat |>
  select(pp_depress_bin, screened, bpg_deprs8, mat_ed) |>
  drop_na()

n_analysis <- nrow(analytic_complete)

biv_tbl <- readRDS(here("results", "tables", "glm_bivariate_screened.rds"))
multi_tbl <- readRDS(here("results", "tables", "glm_multivariable_main.rds"))

model_validation_path <- here("results", "tables", "model_comparison.rds")
if (file.exists(model_validation_path)) {
  model_validation_results <- readRDS(model_validation_path)
} else {
  model_validation_results <- tibble(
    model = c("Logistic regression", "Random forest"),
    .metric = c("accuracy", "roc_auc"),
    mean = c(NA_real_, NA_real_),
    std_err = c(NA_real_, NA_real_),
    n = c(NA_integer_, NA_integer_)
  )
}
```

Disclaimer: The opinions expressed in this article are the author's own and do not reflect their employer.

# Abstract
This study looks at whether screening for mental health issues during pregnancy is related to depression after childbirth, using data from the Pregnancy Risk Assessment Monitoring System (PRAMS) Phase 8. It mainly considers women’s self-reports about their prenatal care and mental health afterward.
The study used basic statistics and logistic regression to see if there’s a link between screening and feelings of depression, taking into account other important factors. It found that women who were screened had slightly lower chances of experiencing postpartum depression, but the difference was small. Since the data is based on self-reports and observational, the results should be viewed with caution.

# Introduction
Postpartum depression is an important health issue that impacts a mother's well-being, family life, and the health of the baby @bauman2020vitalsigns. Guidelines now suggest regularly checking for depression and anxiety during pregnancy and after giving birth period @acog2023screening; @uspstf2019perinatal.
Despite these recommendations, it is still unclear whether screening for mental health during pregnancy leads to fewer cases of postpartum depression across the population. Most of the evidence so far comes from clinical or intervention settings, with less data from broader surveillance. 
Prenatal and postpartum mental health are important parts of a mother's overall health. Poor mental health during this time can lead to problems for both mothers and babies, such as difficulty in caring for oneself and challenges in early child development @acog2023screening. As a result, providers are encouraged to assess mental health throughout pregnancy and after delivery.
Screening during pregnancy may help identify individuals who are already experiencing symptoms or who are at higher risk of postpartum depression. However, population-level evidence on how prenatal screening relates to postpartum depressive symptoms remains limited, and understanding this relationship could help inform both clinical practice and public health policy.
Data for this study come from PRAMS, a population-based surveillance system conducted by the Centers for Disease Control and Prevention in collaboration with state health departments @cdc_prams_phase8_codebook; @cdc_prams_phase8_topics. PRAMS collects self-reported survey data from individuals who recently had a live birth and links responses to selected birth certificate variables. The survey covers maternal experiences before, during, and shortly after pregnancy, including prenatal care, health behaviors, mental health, and postpartum outcomes.
Forty-six states, the District of Columbia, New York City, the Northern Mariana Islands, and Puerto Rico participate in PRAMS. These areas account for 81% of all live births in the United States. PRAMS Phase 8 has many questions and thousands of responses from people in participating states, making it useful for exploring data and building models.

# Research Question
Are women who are asked about depression or anxiety during prenatal visits less likely to experience postpartum depression? 
The main outcome is whether women report feelings of depression or losing interest since giving birth, based on survey questions. The main factor studied is whether healthcare providers asked about mental health during pregnancy. Other factors include depression during pregnancy and the mother's education level.
# Methods
## Use of Computational Assistance
ChatGPT was used to help troubleshoot parts of the code. All analyses, decisions, and interpretations were reviewed and completed by the author.
## Analysis Approach
The analysis started by comparing people who reported having prenatal mental health screening with those who did not. This helped us check the data for quality, find missing details, and identify early patterns. We then used statistical methods to explore the relationship between prenatal screening and postpartum depression, taking into account important factors.
## Data Source
Data came from PRAMS, a survey system run by the Centers for Disease Control and Prevention along with state health departments. PRAMS gathers information that people report about their experiences before, during, and shortly after pregnancy.
Because PRAMS uses a detailed survey method, the data can be analyzed using special variables included in the dataset. The analyses shown here are unweighted and intended as a simple example of how to build models so others can repeat them.

## Study Population
The full PRAMS analytic dataset included `r scales::comma(n_total)` respondents. After excluding observations with missing values for the primary exposure, outcome, and selected covariates, the final analytic sample included `r scales::comma(n_analysis)` respondents. Complete case analysis was used, which might cause bias if data are not missing completely at random. Respondents with missing data might differ in important ways from those with complete data, potentially affecting how well the results apply to other groups.
```{r}
#| label: tbl-study-pop
#| tbl-cap: "Study population characteristics"

study_pop_tbl <- tibble(
  Characteristic = c(
    "Sample size",
    "  Total respondents",
    "  Respondents in analytic sample",
    "Prenatal screening",
    "  Screened during prenatal care",
    "  Not screened during prenatal care",
    "Postpartum depressive symptoms",
    "  Yes",
    "  No",
    "Depression during pregnancy",
    "  No",
    "  Yes",
    "Maternal education",
    "  Less than high school",
    "  High school graduate",
    "  Some college",
    "  College graduate",
    "  More than college"
  ),
  n = c(
    "",
    scales::comma(n_total),
    scales::comma(n_analysis),
    "",
    scales::comma(sum(analytic_complete$screened == 1)),
    scales::comma(sum(analytic_complete$screened == 0)),
    "",
    scales::comma(sum(analytic_complete$pp_depress_bin == 0)),
    scales::comma(sum(analytic_complete$pp_depress_bin == 1)),
    "",
    scales::comma(sum(analytic_complete$bpg_deprs8 == 1)),
    scales::comma(sum(analytic_complete$bpg_deprs8 == 2)),
    "",
    scales::comma(sum(analytic_complete$mat_ed == 1)),
    scales::comma(sum(analytic_complete$mat_ed == 2)),
    scales::comma(sum(analytic_complete$mat_ed == 3)),
    scales::comma(sum(analytic_complete$mat_ed == 4)),
    scales::comma(sum(analytic_complete$mat_ed == 5))
  ),
  Percent = c(
    "",
    "100.0%",
    paste0(round(100 * n_analysis / n_total, 1), "%"),
    "",
    paste0(round(100 * sum(analytic_complete$screened == 1) / n_analysis, 1), "%"),
    paste0(round(100 * sum(analytic_complete$screened == 0) / n_analysis, 1), "%"),
    "",
    paste0(round(100 * sum(analytic_complete$pp_depress_bin == 0) / n_analysis, 1), "%"),
    paste0(round(100 * sum(analytic_complete$pp_depress_bin == 1) / n_analysis, 1), "%"),
    "",
    paste0(round(100 * sum(analytic_complete$bpg_deprs8 == 1) / n_analysis, 1), "%"),
    paste0(round(100 * sum(analytic_complete$bpg_deprs8 == 2) / n_analysis, 1), "%"),
    "",
    paste0(round(100 * sum(analytic_complete$mat_ed == 1) / n_analysis, 1), "%"),
    paste0(round(100 * sum(analytic_complete$mat_ed == 2) / n_analysis, 1), "%"),
    paste0(round(100 * sum(analytic_complete$mat_ed == 3) / n_analysis, 1), "%"),
    paste0(round(100 * sum(analytic_complete$mat_ed == 4) / n_analysis, 1), "%"),
    paste0(round(100 * sum(analytic_complete$mat_ed == 5) / n_analysis, 1), "%")
  )
)

knitr::kable(study_pop_tbl, align = c("l", "r", "r"), booktabs = TRUE) |>
  kableExtra::kable_styling(full_width = FALSE, position = "left", font_size = 11)
```

## Measures
The main exposure was whether a healthcare provider talked about depression or anxiety during prenatal visits, based on a survey question. This was recorded as yes or no.
The main outcome was postpartum depressive symptoms, measured using survey questions about feelings of depression and loss of interest after childbirth. These questions have been used before in research on postpartum depression @bauman2020vitalsigns; @farr2016provider.
Key factors were depression during pregnancy and the mother's education level. Depression during pregnancy was included because women who have had depression before are more likely to have depressive symptoms after giving birth. The mother's education was included because it can affect access to healthcare and mental health.

## Statistical Approach
Exploratory analyses involved looking at basic statistics, cross-tabulation, and bar charts to compare postpartum depression symptoms based on screening status. All work was done using R.
A multivariable logistic regression was used to analyze whether postpartum depression symptoms were linked to prenatal mental health screening. Depression during pregnancy and maternal education were included as additional factors. Results are shown as odds ratios with 95% confidence intervals.

A random forest approach was also used alongside logistic regression because it can handle complex relationships between variables without needing to specify them in advance. To see if this method improved prediction, both models were tested using 5-fold cross-validation. The random forest used the ranger package with 300 trees and default settings for other options. The performance of the models was measured by accuracy and ROC AUC, with averages and variation across the tests used to check stability.

# Results
The analytic sample included `r scales::comma(n_analysis)` respondents. Overall, `r round(100 * sum(analytic_complete$screened == 1) / n_analysis, 1)`% reported being screened for depression during prenatal care, and `r round(100 * sum(analytic_complete$pp_depress_bin == 1) / n_analysis, 1)`% reported postpartum depressive symptoms.

## Exploratory Analysis
Postpartum depression was a bit more common among women who were not screened, at 15.2%, compared to 13.9% for those who were screened. Higher rates were also seen in women who experienced depression during pregnancy, and differences between various education levels were visible.

```{r}
#| label: tbl-bivariate
#| tbl-cap: "Unadjusted odds ratio for prenatal mental health screening and postpartum depressive symptoms"

biv_clean <- biv_tbl |>
  filter(term == "screened1") |>
  transmute(
    Predictor = "Prenatal mental health screening",
    `Odds ratio` = sprintf("%.2f", estimate),
    `95% CI` = paste0("(", sprintf("%.2f", conf.low), ", ", sprintf("%.2f", conf.high), ")"),
    `P value` = ifelse(p.value < 0.001, "<0.001", sprintf("%.3f", p.value))
  )

knitr::kable(
  biv_clean,
  col.names = c("Predictor", "Odds ratio", "95% CI", "P value"),
  align = c("l", "c", "c", "c"),
  booktabs = TRUE
) |>
  kableExtra::kable_styling(full_width = FALSE, position = "left") |>
  kableExtra::footnote(
    general = "Unadjusted model. Reference group: not screened during prenatal care.",
    general_title = "Note:",
    footnote_as_chunk = TRUE
  )
```
Before considering other factors, the respondents who were screened already had lower chances of postpartum depression compared to those who were not screened.

```{r}
#| label: tbl-multivariable
#| tbl-cap: "Adjusted odds ratios for factors associated with postpartum depressive symptoms"
multi_clean <- multi_tbl |>
  filter(term != "(Intercept)") |>
  mutate(
    Predictor = case_when(
      term == "screened1" ~ "Prenatal mental health screening",
      term == "bpg_deprs82" ~ "Depression during pregnancy",
      term == "mat_ed2" ~ "Education: High school graduate",
      term == "mat_ed3" ~ "Education: Some college",
      term == "mat_ed4" ~ "Education: College graduate",
      term == "mat_ed5" ~ "Education: More than college",
      TRUE ~ term
    ),
    OR = estimate,
    CI_low = conf.low,
    CI_high = conf.high
  ) |>
  transmute(
    Predictor,
    `Odds ratio` = sprintf("%.2f", OR),
    `95% CI` = paste0("(", sprintf("%.2f", CI_low), ", ", sprintf("%.2f", CI_high), ")"),
    `P value` = ifelse(p.value < 0.001, "<0.001", sprintf("%.3f", p.value))
  )

knitr::kable(
  multi_clean,
  col.names = c("Predictor", "Odds ratio", "95% CI", "P value"),
  align = c("l", "c", "c", "c"),
  booktabs = TRUE
) |>
  kableExtra::kable_styling(full_width = FALSE, position = "left") |>
  kableExtra::footnote(
    general = "Reference groups: not screened during prenatal care, no depression during pregnancy, less than high school education.",
    general_title = "Note:",
    footnote_as_chunk = TRUE
  )
```

```{r}
#| label: fig-screening
#| fig-cap: "Postpartum depressive symptoms by prenatal mental health screening"

fig_data <- analytic_complete |>
  count(screened, pp_depress_bin) |>
  group_by(screened) |>
  mutate(percent = 100 * n / sum(n)) |>
  ungroup() |>
  mutate(
    Screening = ifelse(screened == 1, "Screened", "Not screened"),
    Outcome = ifelse(pp_depress_bin == 0, "Depressive symptoms", "No depressive symptoms")
  )

ggplot(fig_data, aes(x = Screening, y = percent, fill = Outcome)) +
  geom_col(width = 0.6) +
  geom_text(
    aes(label = paste0(round(percent, 1), "%")),
    position = position_stack(vjust = 0.5),
    size = 3
  ) +
  scale_fill_manual(
    values = c(
      "Depressive symptoms" = "#D55E00",
      "No depressive symptoms" = "#0072B2"
    )
  ) +
  labs(
    x = "Prenatal mental health screening",
    y = "Percent of respondents",
    fill = "Outcome"
  ) +
  scale_y_continuous(labels = function(x) paste0(x, "%")) +
  theme_minimal() +
  theme(
    legend.position = "right",
    axis.text = element_text(size = 10),
    axis.title = element_text(size = 11)
  )
```

```{r}
#| label: fig-depression-pregnancy
#| fig-cap: "Postpartum depressive symptoms by depression during pregnancy"

fig_dep_preg <- analytic_complete |>
  filter(bpg_deprs8 %in% c(1, 2), pp_depress_bin %in% c(0, 1)) |>
  count(bpg_deprs8, pp_depress_bin) |>
  group_by(bpg_deprs8) |>
  mutate(percent = 100 * n / sum(n)) |>
  ungroup() |>
  mutate(
    Depression_pregnancy = ifelse(bpg_deprs8 == 2, "Yes", "No"),
    Outcome = ifelse(pp_depress_bin == 0, "Depressive symptoms", "No depressive symptoms")
  )

ggplot(fig_dep_preg, aes(x = Depression_pregnancy, y = percent, fill = Outcome)) +
  geom_col(width = 0.6) +
  geom_text(
    aes(label = paste0(round(percent, 1), "%")),
    position = position_stack(vjust = 0.5),
    size = 3
  ) +
  scale_fill_manual(
    values = c(
      "Depressive symptoms" = "#D55E00",
      "No depressive symptoms" = "#0072B2"
    )
  ) +
  scale_y_continuous(labels = function(x) paste0(x, "%")) +
  labs(
    x = "Depression during pregnancy",
    y = "Percent of respondents",
    fill = "Outcome"
  ) +
  theme_minimal() +
  theme(
    legend.position = "right",
    axis.text = element_text(size = 10),
    axis.title = element_text(size = 11)
  )

dir.create(here("results", "figures"), showWarnings = FALSE, recursive = TRUE)

ggsave(
  filename = here("results", "figures", "depression_pregnancy_vs_postpartum.png"),
  plot = last_plot(),
  width = 8,
  height = 5,
  dpi = 300
)
```
Women who experience depression during pregnancy are more likely to have postpartum depression. About 31 out of 100 women with depression during pregnancy also had postpartum depression, compared to about 11 out of 100 women who did not experience depression.

```{r}
#| label: tbl-screening
#| tbl-cap: "Distribution of postpartum depressive symptoms by prenatal mental health screening"

screening_table <- analytic_complete |>
  count(screened, pp_depress_bin) |>
  group_by(screened) |>
  mutate(
    percent = round(100 * n / sum(n), 1),
    Screening = ifelse(screened == 1, "Screened", "Not screened"),
    Outcome = ifelse(pp_depress_bin == 0, "Depressive symptoms", "No depressive symptoms")
  ) |>
  ungroup() |>
  mutate(value = paste0(scales::comma(n), " (", percent, "%)")) |>
  select(Screening, Outcome, value) |>
  pivot_wider(names_from = Outcome, values_from = value) |>
  rename(`Screening status` = Screening)

knitr::kable(screening_table, align = c("l", "c", "c"))
```

Table 4 shows that 15.2% of respondents who were not screened reported postpartum depression symptoms, compared to 13.9% of those who were screened. This aligns with the results from the adjusted model.

```{r}
#| label: tbl-model-validation
#| tbl-cap: "Model validation results comparing logistic regression and random forest"

model_validation_tbl <- model_validation_results |>
  filter(.metric %in% c("accuracy", "roc_auc")) |>
  mutate(
    metric_label = case_when(
      .metric == "accuracy" ~ "Accuracy",
      .metric == "roc_auc" ~ "ROC AUC",
      TRUE ~ .metric
    ),
    value = paste0(round(mean, 3), " (SE ", round(std_err, 3), ")")
  ) |>
  select(model, metric_label, value) |>
  pivot_wider(names_from = metric_label, values_from = value) |>
  rename(Model = model)

knitr::kable(model_validation_tbl, align = c("l", "c", "c"))
```

```{r}
#| label: fig-model-performance
#| fig-cap: "Cross-validation performance of logistic regression and random forest"

model_validation_plot <- model_validation_results |>
  filter(.metric %in% c("accuracy", "roc_auc")) |>
  mutate(
    metric_label = case_when(
      .metric == "accuracy" ~ "Accuracy",
      .metric == "roc_auc" ~ "ROC AUC",
      TRUE ~ .metric
    ),
    model = case_when(
      model == "logistic_regression" ~ "Logistic regression",
      model == "random_forest" ~ "Random forest",
      TRUE ~ str_replace_all(model, "_", " ") |> str_to_title()
    )
  )

ggplot(model_validation_plot, aes(x = model, y = mean)) +
  geom_col(width = 0.6, fill = "steelblue") +
  geom_errorbar(
    aes(ymin = mean - std_err, ymax = mean + std_err),
    width = 0.15
  ) +
  facet_wrap(~ metric_label, scales = "free_y") +
  labs(
    x = "Model",
    y = "Cross-validated mean performance"
  ) +
  coord_cartesian(ylim = c(0.6, 0.9)) +
  geom_text(aes(label = round(mean, 3)), vjust = -0.5, size = 3) +
  theme_minimal() +
  theme(
    strip.text = element_text(size = 10),
    axis.text = element_text(size = 10),
    axis.title = element_text(size = 11),
    axis.text.x = element_text(angle = 20, hjust = 1)
  )
```

Both models performed very similarly, with an accuracy of 0.859 and a ROC AUC of 0.656. The small differences in error across the different tests suggest the results are consistent and not affected much by which data points were left out. A ROC AUC of 0.656 shows only a moderate ability to tell the differences, which isn't surprising given the small number of factors used. Since the random forest didn't do better than logistic regression, we kept the simpler model.
In the adjusted model, women who had prenatal mental health screening were less likely to experience postpartum depression compared to those who weren't screened (OR = 0.76, 95% CI: 0.74 to 0.78, p < 0.001), even after taking into account depression during pregnancy and maternal education.

# Discussion
This study examined whether screening for mental health issues during pregnancy was linked to lower chances of experiencing depression after childbirth, using PRAMS data. Women who were screened had slightly lower chances of postpartum depression even after considering previous depression and education levels. However, because this was an observational study, we can't say for sure that screening caused the lower depression rates. One possible reason is that screening might make women more aware of mental health concerns and encourage them to seek help, connecting them with support before they give birth. Another possibility is that screening detects women who were already more vulnerable to depression, which might show up later regardless of any action taken.
These findings are broadly consistent with prior work showing that postpartum depressive symptoms are common and that provider screening practices vary @bauman2020vitalsigns; @farr2016provider. Clinical guidelines recommend routine screening during pregnancy and postpartum @acog2023screening, and these results add to the population-level evidence on how screening relates to reported symptoms.
The most noticeable finding was how strongly depression during pregnancy predicted postpartum depression symptoms (e.g., women with a history of depression were almost three times more likely to experience postpartum symptoms). This highlights the importance of considering mental health history when studying postpartum outcomes.
It’s important to understand what this study cannot show. Since the data are based on a single point in time and rely on self-reports, we can't say whether screening itself led to better results. Women who were screened might also have had better access to healthcare or more supportive providers, which could influence postpartum mental health independently.
**Strengths:** PRAMS is a large dataset from multiple states, which provides enough information for detailed analysis and makes the results more applicable to different areas than studies limited to a single site.
**Limitations:** The data are based on what participants reported themselves and might be affected by memory errors. The measure used for postpartum depression symptoms is not a hospital diagnosis. Because the study looks at data from one point in time, it’s hard to say what causes what. Factors we did not measure, like whether someone has insurance, social support, or the way healthcare providers offer care, might influence the results.
In future studies, including details such as insurance status, access to mental health care, and income level could be helpful. Using the survey’s weighting factors would also give a better picture of how the results apply to the wider population.

# References

